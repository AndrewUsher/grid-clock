const allNodeList = document.querySelectorAll('clock *')
const ALL_ELEMENTS = [...allNodeList]
const PREFIX_ELEMENTS = document.querySelectorAll('.prefix')
const SUFFIX_ELEMENTS = document.querySelectorAll('.suffix')

export { ALL_ELEMENTS, PREFIX_ELEMENTS, SUFFIX_ELEMENTS }
